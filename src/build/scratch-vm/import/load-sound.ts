const loadSoundFromAsset = function (sound: any): Promise<any> {
  return Promise.resolve(sound);
};

const loadSound = function (sound: any): Promise<any> {
  return Promise.resolve(sound);
};

export {
  loadSound,
  loadSoundFromAsset
};
