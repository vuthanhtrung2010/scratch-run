const deserializeSound = function (): Promise<null> {
  return Promise.resolve(null);
};

const deserializeCostume = function (): Promise<null> {
  return Promise.resolve(null);
};

export {
  deserializeSound,
  deserializeCostume
};
